{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(cat - | jq -r '.tool_input.file_path') && cd \"$CLAUDE_PROJECT_DIR\" && timeout 15 bash -c 'pnpm exec oxlint --fix \"$1\" 2>&1; pnpm exec oxfmt \"$1\" 2>&1' -- \"$FILE\" | tail -20"
          }
        ]
      }
    ]
  },
  "permissions": {
    "deny": ["Bash(git commit --no-verify:*)", "Bash(git commit -n:*)"]
  }
}
