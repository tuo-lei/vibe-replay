{
  "name": "@vibe-replay/cloudflare",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute vibe-replay-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "better-auth": "^1.5.5",
    "drizzle-orm": "^0.44.0",
    "hono": "^4.12.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260305.1",
    "drizzle-kit": "^0.31.0",
    "wrangler": "^4.0.0"
  }
}
